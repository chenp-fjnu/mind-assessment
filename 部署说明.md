# 心智测验 - H5 部署说明

## 项目概述

本项目已完成 UniApp 跨平台重构，支持小程序（微信）和 H5 双端运行。

## 已完成功能

### 核心页面
| 页面 | 说明 |
|------|------|
| 启动页 | 品牌展示，自动跳转首页 |
| 首页 (Hub) | 测验分类展示、脑力游戏入口、最近记录 |
| 分类页 | 按类别浏览测验（智力/人格/情绪/职业/自我） |
| 测验执行 (Runner) | 支持选择题、量表题、矩阵题、数字题 |
| 报告页 | 得分展示、维度分析、结果解读、分享功能 |
| 支付页 | 微信支付/支付宝，演示模式 |
| 历史记录 | 测验和游戏记录筛选查看 |
| 登录页 | 小程序授权/微信H5授权/手机号登录 |
| 隐私政策 | 完整的隐私政策文本 |

### 8个脑力游戏
| 游戏 | 训练能力 |
|------|----------|
| 舒尔特方格 | 专注力 |
| 斯特鲁普效应 | 抑制控制 |
| 数字广度 | 短期记忆 |
| N回溯 | 工作记忆 |
| 反应速度 | 处理速度 |
| 图形记忆 | 视觉记忆 |
| 数字序列 | 逻辑推理 |
| 空间旋转 | 空间认知 |

## 构建输出

H5 构建产物位于 `h5-build/` 目录下：
- `index.html` - 入口文件
- `assets/` - JS/CSS 资源
- `static/` - 静态资源

## 本地测试

```bash
cd h5-build
python -m http.server 8080
```

访问 http://localhost:8080

## 部署方式

### 方式一：静态托管（推荐）
将 `h5-build/` 目录内容上传至：
- Nginx / Apache 服务器
- CDN（阿里云 OSS、腾讯云 COS）
- Vercel / Netlify

### 方式二：uniCloud 前端网页托管
```bash
cd mind-assessment-uniapp
npm run build:h5
# 使用 HBuilderX 上传至 uniCloud
```

### 方式三：Docker 部署
```dockerfile
FROM nginx:alpine
COPY h5-build /usr/share/nginx/html
EXPOSE 80
```

## 环境配置

### H5 微信授权登录
修改 `src/utils/auth.ts` 中的 `YOUR_WECHAT_APPID` 为实际 AppID：
```typescript
const appId = 'YOUR_WECHAT_APPID'
```

### API 域名
修改 `src/stores/app.ts` 中的 `baseUrl`：
```typescript
baseUrl.value = 'https://your-api-domain.com'
```

### 小程序 AppID
修改 `src/manifest.json`：
```json
"mp-weixin": {
    "appid": "your-wx-appid"
}
```

## 构建命令

```bash
# H5 开发
npm run dev:h5

# H5 生产构建
npm run build:h5

# 微信小程序
npm run dev:mp-weixin
npm run build:mp-weixin
```

## 技术栈

- Vue 3 + Composition API
- TypeScript
- UniApp (跨平台)
- Pinia (状态管理)
- Vite (构建工具)
