{
  "name": "mind-assessment-tests",
  "version": "1.0.0",
  "description": "心智测评中心 - 测试体系（模块单元测试 + API集成测试）",
  "private": true,
  "scripts": {
    "test": "jest --verbose --coverage",
    "test:frontend": "jest --verbose --config jest.frontend.config.js",
    "test:server": "jest --verbose --config jest.server.config.js",
    "test:watch": "jest --watch --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jsonwebtoken": "^9.0.3",
    "supertest": "^7.2.2",
    "winston": "^3.19.0"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.0"
  }
}
